---
import ProjectCardModal from './ProjectCardModal.jsx';
import { IProjectDetails } from "@types";

const { projectDetail } = Astro.props as IProjectDetails;
const { title, thumbnails } = projectDetail;
---

<ProjectCardModal client:load thumbnails={thumbnails} title={title} />
