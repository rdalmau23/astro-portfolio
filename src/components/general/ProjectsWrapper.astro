---
import { IProjects } from "@types";
import ProjectCard from "./ProjectCard.astro";

const { projects } = Astro.props as IProjects;
---

<div class="grid  grid-cols-1 gap-9 my-16">
  {projects.map((project) => <ProjectCard projectDetail={project} />)}
</div>
